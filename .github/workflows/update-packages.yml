name: Update Packages

on:
  schedule:
    - cron: '5 12 * * *' # 06:05 CST (UTC-6)
  workflow_dispatch:

jobs:
  update:
    uses: ./.github/workflows/_shared/update-packages.yml
    secrets: inherit
