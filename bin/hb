#!/usr/bin/env bash
# Wrapper for the Honeybadger CLI with project-local config.
# Requires: brew install honeybadger-io/tap/honeybadger
# Config is generated by bin/setup-credentials.
exec hb --config "$(dirname "${BASH_SOURCE[0]}")/../.honeybadger-cli.yaml" "$@"
